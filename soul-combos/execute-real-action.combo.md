---
name: Execute Real Action
slug: execute_real_action
description: Executes a validated plan in the real environment, potentially involving multiple steps like file modifications, tool invocations, and deployments.

params:
  - name: executionPlan
    type: string # This would be a structured plan generated by SKILL - Plan from Simulation
    description: "The detailed, validated plan of actions to execute in the real environment."
  - name: targetEnvironment
    type: string
    description: "The environment (e.g., 'development', 'staging') where the actions should be executed."

skills:
  - name: SKILL - Apply File Modifications
    slug: apply_file_modifications # Placeholder
    description: "Applies any necessary file changes as per the execution plan."
    input: "executionPlan"
    output: "modified_files_status"

  - name: SKILL - Tool Invocation Orchestrator
    slug: tool_invocation_orchestrator # Placeholder
    description: "Orchestrates the invocation of specific tools or scripts required by the plan."
    input: "executionPlan"
    output: "tool_execution_results"
    
  - name: SKILL - Deployment Executor
    slug: deployment_executor # Placeholder
    description: "Executes deployment steps if the plan involves deploying a feature or change."
    input: "executionPlan"
    params:
      environment: "{{targetEnvironment}}"
    output: "deployment_status"

  - name: SKILL - Status Reporting
    slug: status_reporting # Placeholder
    description: "Reports the outcome of the real actions to the user or relevant monitoring system."
    input:
      - "modified_files_status"
      - "tool_execution_results"
      - "deployment_status"
    output: "final_action_report"

error_handling:
  on_failure: "halt"
  on_success: "notify_user"
  message: "Execute Real Action combo {{status}} at step: {{failed_skill}}. Details: {{error_message}}"
---