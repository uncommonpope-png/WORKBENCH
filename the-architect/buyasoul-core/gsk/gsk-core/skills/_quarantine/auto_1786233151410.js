<tool_call>
{"tool": "write_file", "path": "C:\\Users\\Craig\\gsk-core\\skills\\auto_1786233005286.js", "content": "/**\n * auto_1786233005286 — Spatial Audio + Multi-Agent Handoff + WebSocket Sync Skill\n * Synthesizes: real-time spatial audio rendering, autonomous agent handoff patterns, WebSocket state sync\n */\n\nconst MANIFEST = {\n  name: \"auto_1786233005286\",\n  version: \"1.0.0\",\n  description: \"Unifies spatial audio, agent handoff, and WebSocket state synchronization for real-time multi-agent environments\",\n  tags: [\"webaudio\", \"spatial-audio\", \"multi-agent\", \"handoff\", \"websocket\", \"state-sync\", \"game-engine\"],\n  inputs: {\n    type: \"object\",\n    properties: {\n      action: { type: \"string\", enum: [\"spatial-audio\", \"agent-handoff\", \"websocket-sync\", \"full-pipeline\"] },\n      audioConfig: { type: \"object\" },\n      agents: { type: \"array\", items: { type: \"object\" } },\n      gameState: { type: \"object\" },\n      websocketUrl: { type: \"string\" }\n    },\n    required: [\"action\"]\n  },\n  outputs: { type: \"string\" }\n};\n\nconst PLT_AFFINITY = {\n  profit: 0.4,\n  love: 0.3,\n  tax: 0.3\n};\n\nclass SpatialAudioEngine {\n  constructor(config = {}) {\n    this.audioContext = null;\n    this.pannerNodes = new Map();\n    this.listener = null;\n    this.config = {\n      maxDistance: config.maxDistance || 100,\n      rolloffFactor: config.rolloffFactor || 1,\n      coneInnerAngle: config.coneInnerAngle || 360,\n      coneOuterAngle: config.coneOuterAngle || 360,\n      coneOuterGain: config.coneOuterGain || 0\n    };\n  }\n\n  async initialize() {\n    if (typeof AudioContext !== 'undefined') {\n      this.audioContext = new AudioContext();\n    } else if (typeof webkitAudioContext !== 'undefined') {\n      this.audioContext = new webkitAudioContext();\n    }\n    this.listener = this.audioContext.listener;\n    return this.audioContext ? \"initialized\" : \"failed\";\n  }\n\n  createSpatialSource(id, position, audioBuffer) {\n    const source = this.audioContext.createBufferSource();\n    const panner = this.audioContext.createPanner();\n    const gain = this.audioContext.createGain();\n\n    panner.panningModel = 'HRTF';\n    panner.distanceModel = 'inverse';\n    panner.maxDistance = this.config.maxDistance;\n    panner.rolloffFactor = this.config.rolloffFactor;\n    panner.coneInnerAngle = this.config.coneInnerAngle;\n    panner.coneOuterAngle = this.config.coneOuterAngle;\n    panner.coneOuterGain = this.config.coneOuterGain;\n\n    panner.positionX.setValueAtTime(position.x, this.audioContext.currentTime);\n    panner.positionY.setValueAtTime(position.y, this.audioContext.currentTime);\n    panner.positionZ.setValueAtTime(position.z, this.audioContext.currentTime);\n\n    source.buffer = audioBuffer;\n    source.connect(panner).connect(gain).connect(this.audioContext.destination);\n\n    this.pannerNodes.set(id, { source, panner, gain, position });\n    return id;\n  }\n\n  updateSourcePosition(id, position) {\n    const node = this.pannerNodes.get(id);\n    if (!node) return false;\n    const t = this.audioContext.currentTime;\n    node.panner.positionX.setValueAtTime(position.x, t);\n    node.panner.positionY.setValueAtTime(position.y, t);\n    node.panner.positionZ.setValueAtTime(position.z, t);\n    node.position = position;\n    return true;\n  }\n\n  updateListener(position, orientation) {\n    const t = this.audioContext.currentTime;\n    this.listener.positionX.setValueAtTime(position.x, t);\n    this.listener.positionY.setValueAtTime(position.y, t);\n    this.listener.positionZ.setValueAtTime(position.z, t);\n    if (orientation) {\n      this.listener.forwardX.setValueAtTime(orientation.forwardX, t);\n      this.listener.forwardY.setValueAtTime(orientation.forwardY, t);\n      this.listener.forwardZ.setValueAtTime(orientation.forwardZ, t);\n      this.listener.upX.setValueAtTime(orientation.upX, t);\n      this.listener.upY.setValueAtTime(orientation.upY, t);\n      this.listener.upZ.setValueAtTime(orientation.upZ, t);\n    }\n  }\n\n  play(id) {\n    const node = this.pannerNodes.get(id);\n    if (!node) return false;\n    node.source.start();\n    return true;\n  }\n\n  stop(id) {\n    const node = this.pannerNodes.get(id);\n    if (!node) return false;\n    node.source.stop();\n    this.pannerNodes.delete(id);\n    return true;\n  }\n}\n\nclass AgentHandoffManager {\n  constructor() {\n    this.agents = new Map();\n    this.handoffHistory = [];\n    this.activeHandoff = null;\n  }\n\n  registerAgent(agent) {\n    this.agents.set(agent.id, {\n      ...agent,\n      state: 'idle',\n      capabilities: agent.capabilities || [],\n      lastHeartbeat: Date.now()\n    });\n  }\n\n  async initiateHandoff(fromAgentId, toAgentId, context, reason) {\n    const fromAgent = this.agents.get(fromAgentId);\n    const toAgent = this.agents.get(toAgentId);\n\n    if (!fromAgent || !toAgent) {\n      throw new Error('Agent not found');\n    }\n\n    const handoff = {\n      id: `handoff_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,\n      from: fromAgentId,\n      to: toAgentId,\n      context,\n      reason,\n      status: 'initiated',\n      timestamp: Date.now(),\n      ackTimeout: 5000\n    };\n\n    this.activeHandoff = handoff;\n    fromAgent.state = 'handing-off';\n    toAgent.state = 'receiving';\n\n    return handoff;\n  }\n\n  async confirmHandoff(handoffId, success, result) {\n    if (!this.activeHandoff || this.activeHandoff.id !== handoffId) {\n      return false;\n    }\n\n    const { from, to } = this.activeHandoff;\n    const fromAgent = this.agents.get(from);\n    const toAgent = this.agents.get(to);\n\n    this.activeHandoff.status = success ? 'completed' : 'failed';\n    this.activeHandoff.result = result;\n    this.activeHandoff.completedAt = Date.now();\n\n    this.handoffHistory.push(this.activeHandoff);\n\n    fromAgent.state = success ? 'idle' : 'error';\n    toAgent.state = success ? 'active' : 'idle';\n\n    this.activeHandoff = null;\n    return true;\n  }\n\n  getHandoffHistory() {\n    return this.handoffHistory;\n  }\n\n  getAgentStates() {\n    const states = {};\n    for (const [id, agent] of this.agents) {\n      states[id] = { state: agent.state, capabilities: agent.capabilities };\n    }\n    return states;\n  }\n}\n\nclass WebSocketStateSync {\n  constructor(url) {\n    this.url = url;\n    this.ws = null;\n    this.state = {};\n    this.pendingAcks = new Map();\n    this.sequence = 0;\n    this.listeners = new Map();\n    this.reconnectAttempts = 0;\n    this.maxReconnectAttempts = 5;\n    this.reconnectDelay = 1000;\n  }\n\n  connect() {\n    return new Promise((resolve, reject) => {\n      this.ws = new WebSocket(this.url);\n\n      this.ws.onopen = () => {\n        this.reconnectAttempts = 0;\n        this.emit('connected', {});\n        resolve(true);\n      };\n\n      this.ws.onmessage = (event) => {\n        try {\n          const message = JSON.parse(event.data);\n          this.handleMessage(message);\n        } catch (e) {\n          console.error('Invalid message:', e);\n        }\n      };\n\n      this.ws.onclose = () => {\n        this.emit('disconnected', {});\n        this.attemptReconnect();\n      };\n\n      this.ws.onerror = (error) => {\n        this.emit('error', error);\n        reject(error);\n      };\n    });\n  }\n\n  attemptReconnect() {\n    if (this.reconnectAttempts >= this.maxReconnectAttempts) {\n      this.emit('reconnect-failed', {});\n      return;\n    }\n    this.reconnectAttempts++;\n    setTimeout(() => this.connect(), this.reconnectDelay * this.reconnectAttempts);\n  }\n\n  handleMessage(message) {\n    switch (message.type) {\n      case 'state-update':\n        this.applyStateUpdate(message.payload);\n        this.sendAck(message.sequence);\n        break;\n      case 'state-snapshot':\n        this.state = message.payload;\n        this.emit('snapshot', this.state);\n        break;\n      case 'ack':\n        this.handleAck(message.sequence);\n        break;\n      case 'agent-handoff':\n        this.emit('agent-handoff', message.payload);\n        break;\n      case 'audio-update':\n        this.emit('audio-update', message.payload);\n        break;\n    }\n  }\n\n  applyStateUpdate(payload) {\n    this.state = { ...this.state, ...payload };\n    this.emit('state-change', this.state);\n  }\n\n  sendStateUpdate(update) {\n    const seq = ++this.sequence;\n    const message = {\n      type: 'state-update',\n      sequence: seq,\n      payload: update,\n      timestamp: Date.now()\n    };\n    this.pendingAcks.set(seq, { message, timestamp: Date.now(), retries: 0 });\n    this.ws.send(JSON.stringify(message));\n    return seq;\n  }\n\n  sendAck(sequence) {\n    this.ws.send(JSON.stringify({ type: 'ack', sequence, timestamp: Date.now() }));\n  }\n\n  handleAck(sequence) {\n    this.pendingAcks.delete(sequence);\n  }\n\n  requestSnapshot() {\n    this.ws.send(JSON.stringify({ type: 'snapshot-request', timestamp: Date.now() }));\n  }\n\n  on(event, callback) {\n    if (!this.listeners.has(event)) {\n      this.listeners.set(event, []);\n    }\n    this.listeners.get(event).push(callback);\n  }\n\n  emit(event, data) {\n    const callbacks = this.listeners.get(event) || [];\n    callbacks.forEach(cb => cb(data));\n  }\n\n  getState() {\n    return { ...this.state };\n  }\n\n  close() {\n    if (this.ws) {\n      this.ws.close();\n      this.ws = null;\n    }\n  }\n}\n\nasync function execute(input) {\n  const { action, audioConfig, agents, gameState, websocketUrl } = input;\n\n  try {\n    switch (action) {\n      case 'spatial-audio': {\n        const engine = new SpatialAudioEngine(audioConfig);\n        await engine.initialize();\n        return JSON.stringify({ status: 'spatial-audio-engine-ready', config: audioConfig });\n      }\n\n      case 'agent-handoff': {\n        const manager = new AgentHandoffManager();\n        if (agents) agents.forEach(a => manager.registerAgent(a));\n        return JSON.stringify({ status: 'handoff-manager-ready', agents: manager.getAgentStates() });\n      }\n\n      case 'websocket-sync': {\n        if (!websocketUrl) throw new Error('websocketUrl required');\n        const sync = new WebSocketStateSync(websocketUrl);\n        await sync.connect();\n        if (gameState) sync.sendStateUpdate(gameState);\n        return JSON.stringify({ status: 'websocket-connected', state: sync.getState() });\n      }\n\n      case 'full-pipeline': {\n        const engine = new SpatialAudioEngine(audioConfig);\n        await engine.initialize();\n\n        const manager = new AgentHandoffManager();\n        if (agents) agents.forEach(a => manager.registerAgent(a));\n\n        const sync = new WebSocketStateSync(websocketUrl);\n        await sync.connect();\n\n        sync.on('agent-handoff', async (payload) => {\n          const handoff = await manager.initiateHandoff(payload.from, payload.to, payload.context, payload.reason);\n          sync.sendStateUpdate({ handoff });\n        });\n\n        sync.on('audio-update', (payload) => {\n          if (payload.action === 'create') {\n            engine.createSpatialSource(payload.id, payload.position, payload.buffer);\n          } else if (payload.action === 'update') {\n            engine.updateSourcePosition(payload.id, payload.position);\n          } else if (payload.action === 'play') {\n            engine.play(payload.id);\n          } else if (payload.action === 'stop') {\n            engine.stop(payload.id);\n          }\n        });\n\n        if (gameState) sync.sendStateUpdate(gameState);\n\n        return JSON.stringify({\n          status: 'full-pipeline-active',\n          components: {\n            spatialAudio: 'ready',\n            agentHandoff: 'ready',\n            websocketSync: 'connected'\n          },\n          initialState: sync.getState()\n        });\n      }\n\n      default:\n        throw new Error(`Unknown action: ${action}`);\n    }\n  } catch (error) {\n    return JSON.stringify({ status: 'error', error: error.message });\n  }\n}\n\nmodule.exports = { execute, MANIFEST, PLT_AFFINITY, SpatialAudioEngine, AgentHandoffManager, WebSocketStateSync };\n"}